// The "Prestage AI" wordmark: "AI" renders muted, inheriting the parent's
// font so every surface sizes it with its own text styles.
export const BrandName = () => (
  <>
    Prestage <span className="text-on-surface-3">AI</span>
  </>
)
